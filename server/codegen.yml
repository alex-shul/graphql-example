overwrite: true
schema: "http://localhost:3000/graphql"
documents: null
generates:
  ../common/model/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
  ../common/model/graphql.schema.json:
    plugins:
      - "introspection"
